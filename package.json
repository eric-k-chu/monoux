{
  "name": "monoux",
  "devDependencies": {
    "@biomejs/biome": "1.9.0",
    "husky": "^9.1.5",
    "prettier": "^3.3.3",
    "prettier-plugin-tailwindcss": "^0.6.6",
    "syncpack": "^13.0.0",
    "turbo": "^2.1.1"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.9.0",
  "private": true,
  "scripts": {
    "add:app": "pnpm --filter @monoux/gen add:app",
    "add:package": "pnpm --filter @monoux/gen add:package",
    "build": "turbo build",
    "clean": "turbo clean",
    "dev": "turbo dev",
    "format": "prettier --write \"**/*.{ts,tsx}\"",
    "lint": "turbo lint",
    "prepare": "husky",
    "test": "turbo test",
    "vert:dev": "turbo vert:dev"
  }
}
